using System;

namespace Project.Models
{
    static class MenageMedApp
    {
        public List<Patient> patientList;
        void addPatient(Patient patient);
        void addMedication(Patient patient, Medication medication);
    }
}